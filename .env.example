# Copy to `.env` and fill in values. Vite exposes only variables prefixed with VITE_.

# Azure Functions base (no trailing slash)
VITE_API_BASE_URL=https://healthchker-akhghwe9adgxcqdt.eastus-01.azurewebsites.net/api

# Function or host key (?code=...) required for HTTP triggers
VITE_API_CODE=
