[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so /dashboard and /manage load index.html (Vue Router history mode).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
